apiVersion: v1
kind: ConfigMap
metadata:
  name: charradissa-config
  namespace: occitan-system
  labels:
    {{- include "occitan.labels" . | nindent 4 }}
data:
  charradissa.toml: |
    [org]
    name = {{ .Values.charradissa.config.org.name | quote }}
    homeserver = {{ .Values.charradissa.config.org.homeserver | quote }}

    [backend]
    type = {{ .Values.charradissa.config.backend.type | quote }}

    [concierge]
    archival_interval_hours = {{ .Values.charradissa.config.concierge.archival_interval_hours }}
    convergence_interval_hours = {{ .Values.charradissa.config.concierge.convergence_interval_hours }}
    daily_token_budget = {{ .Values.charradissa.config.concierge.daily_token_budget }}

    [approval]
    timeout_minutes = {{ .Values.charradissa.config.approval.timeout_minutes }}

    [tasks]
    type = {{ .Values.charradissa.config.tasks.type | quote }}

    [projects]
    autodiscover = {{ .Values.charradissa.config.projects.autodiscover }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: charradissa
  namespace: occitan-system
  labels:
    {{- include "occitan.labels" . | nindent 4 }}
    {{- include "occitan.selectorLabels" (dict "component" "charradissa" "context" .) | nindent 4 }}
spec:
  # Stays at 0 until the Matrix appservice is wired (needs the 'charradissa' secret).
  replicas: {{ if .Values.charradissa.appservice.enabled }}{{ .Values.charradissa.replicas }}{{ else }}0{{ end }}
  selector:
    matchLabels:
      {{- include "occitan.selectorLabels" (dict "component" "charradissa" "context" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "occitan.selectorLabels" (dict "component" "charradissa" "context" .) | nindent 8 }}
    spec:
      # The daemon reads CHARRADISSA_PORT for its listen port, which collides with
      # the kubelet's legacy service-link env var (<SVCNAME>_PORT=tcp://ip:port).
      # Disable service links so the daemon's "8448" default applies. (Code fix:
      # rename the env var to a non-colliding name — tracked for the daemon rebuild.)
      enableServiceLinks: false
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.openbao.enabled }}
      initContainers:
        # Pull SYNAPSE_ADMIN_TOKEN from OpenBao into a shared creds volume so the
        # daemon can drive the Synapse admin API. OpenBao stays the source of truth;
        # nothing is duplicated into a long-lived k8s secret. Mirrors the fetch-tokens
        # pattern in the guilhem chart.
        - name: fetch-tokens
          image: openbao/openbao:latest
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -eu
              export BAO_ADDR=http://openbao:8200
              SYNAPSE_ADMIN=$(bao kv get -field=value secret/occitan/synapse-admin-token)
              umask 077
              cat > /creds/tokens.env <<EOF
              export SYNAPSE_ADMIN_TOKEN='$SYNAPSE_ADMIN'
              EOF
              echo "synapse admin token written to /creds/tokens.env"
          env:
            - name: BAO_TOKEN
              valueFrom:
                secretKeyRef:
                  name: openbao
                  key: token
          volumeMounts:
            - name: creds
              mountPath: /creds
      {{- end }}
      containers:
        - name: charradissa
          image: {{ .Values.charradissa.image }}:{{ .Values.charradissa.imageTag }}
          imagePullPolicy: Always
          # The daemon entrypoint is `charradissa-daemon`; wrap it in a shell so the
          # OpenBao-provided tokens.env is sourced (when present) before exec'ing the
          # daemon, which then inherits SYNAPSE_ADMIN_TOKEN. Mirrors guilhem's wrapper.
          command: ["/bin/sh", "-c"]
          args:
            - |
              [ -f /creds/tokens.env ] && . /creds/tokens.env
              exec charradissa-daemon
          ports:
            - containerPort: {{ .Values.charradissa.port }}
          env:
            {{- range $k, $v := .Values.charradissa.env }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: anthropic
                  key: api-key
            - name: MATRIX_AS_TOKEN
              valueFrom:
                secretKeyRef:
                  name: charradissa
                  key: as-token
            - name: FARGA_URL
              value: "http://farga:{{ .Values.farga.port }}"
          volumeMounts:
            - name: config
              mountPath: /config
            {{- if .Values.openbao.enabled }}
            - name: creds
              mountPath: /creds
            {{- end }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.charradissa.port }}
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              memory: 512Mi
      volumes:
        - name: config
          configMap:
            name: charradissa-config
        {{- if .Values.openbao.enabled }}
        # In-memory scratch volume for the OpenBao-fetched tokens.env (never persisted).
        - name: creds
          emptyDir:
            medium: Memory
        {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: charradissa
  namespace: occitan-system
  labels:
    {{- include "occitan.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "occitan.selectorLabels" (dict "component" "charradissa" "context" .) | nindent 4 }}
  ports:
    - port: {{ .Values.charradissa.port }}
      targetPort: {{ .Values.charradissa.port }}
      name: appservice
