apiVersion: v2
name: occitan
description: Occitan stack — Gardian, Farga, Amassada, Charradissa, Synapse, Element
type: application
version: 0.1.0
appVersion: "phase-II"
# PostgreSQL is an in-chart StatefulSet (templates/postgresql.yaml) using the official
# postgres image — the bitnami subchart images are no longer pullable from Docker Hub.
